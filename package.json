{
  "name": "piproject",
  "version": "0.0.1",
  "description": "webgpio package istaller",
  "main": "app.js",
  "private": "true",
  "config": { "port" : "8080" },
  "scripts" : { "install": "npm install express; npm install jade; npm install stylus; npm install nib; npm install pi-gpio; npm install mongoose; npm install coffee-script " },
  "dependencies": {
    "express": "3.0.0alpha4",
    "jade": "*",
    "stylus": "*",
    "nib": "*",
    "pi-gpio": "*",
    "mongoose": "*"
  }
}
